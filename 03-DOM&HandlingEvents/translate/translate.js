const textPL = 'Kije i kamienie kości mi połamią, ale twoje słowa nigdy mnie nie zranią.';
const textANG = 'Sticks and stones may break my bones, but words will never harm me.'

const translateToPl = (element) => {
  element.innerHTML = textPL;
}

const translateToAng = (element) => {
  element.innerHTML = textANG;
}
